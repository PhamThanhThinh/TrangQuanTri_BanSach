@model IEnumerable<TrangQuanTri_BanSach.Models.Domain.Author>
@{
    
}

<div class="container pt-5">
  
  <div class="row">
    <div class="col-md-6">
      <a class="btn btn-success" href="/Author/Add">Thêm tác giả</a>
    </div>
  </div>

  <div class="row">
    <div class="col-md-6">
      <h3>Trang danh sách tác giả</h3>
      <table class="table table-striped table-bordered table-dark">
        <thead>
          <tr>
            <th>Tên tác giả (Author Name)</th>
            <th>Tính năng (Action, Hành động)</th>
          </tr>
        </thead>
        <tbody>
          @foreach (var item in Model)
          {
            <tr>
              <td>@item.AuthorName</td>
              <td>
                <a class="btn btn-success" href="/Author/Update?id=@item.Id">Sửa</a>
                <a class="btn btn-danger" onclick="return window.confirm('Bạn có chắc muốn xóa nó không?')" href="/Author/Delete?id=@item.Id">Xóa</a>
              </td>
            </tr>
          }
        </tbody>
      </table>
    </div>
  </div>
</div>