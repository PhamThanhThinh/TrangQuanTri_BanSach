tcp 2 => win 11



asp.net webforms (.net framework 4.8) deploy trên IIS
asp.net mvc (.net framework 4.8) deploy trên IIS
asp.net core mvc (.net core 6 8)
blazor (.net core)

deploy trên server là windows

trang quản lý cho trang bán sách
- tạo project: asp.net core mvc
- sửa menu
- tạo cơ sở dữ liệu: code first (update database)
- tạo service

rê trỏ chuột vào phần gạch chân màu xanh lá cây
bấm ctrl .
gen contructor (hàm dựng trong c#)

clean code

blazor hybrid (làm app)
blazor (làm web)

grid
table

wordpress (php)

C -> làm nhúng (điều khiển máy móc, động cơ)
C++ -> game, trí tuệ nhân tạo (ô tô, máy bay)
C# -> app desktop, app android, web, game unity (80% thị trường)
java -> app desktop, app android, web
js -> web, app

học web -> phải biết html, css

cách cũ: khai báo thư viện jquery + code ajax

bước 1: tạo service (thêm/xóa/sửa), khai báo service trong program
bước 2: tạo controller (thêm/xóa/sửa)
bước 3: tạo giao diện (thêm/xóa/sửa)

parameter tham số

MCV

MVC (model-view-controller)

coder -> leader -> tester
tester -> coder -> leader ... -> khách hàng -> report -> coder (quy trình của nhật)

QA (quality assurance) -> khách hàng

pt-5 padding top 5

Microsoft Visual Studio 2022 Community vs2022
chuyên về code backend
frontend (code không ok lắm)
vscode visual studio code

C theo phong cách microsoft (khó code C dùng cho nhúng lắm)
C không thuần
C++ vẫn code theo kiểu bình thường được

C vẫn dùng VSCode cho bền
C thuần trên windows https://www.msys2.org/

IDE tích hợp VS2022, elipse, ... nặng
VSCode: chỉ để edit code, còn chạy thì dùng môi trường windows: console, command line



